create table ASSETS (
    id bigint,
    type varchar(20),
    brand varchar(25),
    model varchar(30),
    serialNo varchar(35),
    warranty varchar(15),
    assetTag varchar(50),
    description varchar(200)
);